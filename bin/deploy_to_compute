#!/usr/bin/env bash
set -euo pipefail

deploy_dir="${HOME}/code/Thesis/deploy"
tarball="$(guix pack -L . -RR -S /relext-bin=bin relation-extraction.git)"

mkdir -p "$deploy_dir"

rsync -avP "$tarball" gleidi@files.techfak.de:/media/remote/gleidi